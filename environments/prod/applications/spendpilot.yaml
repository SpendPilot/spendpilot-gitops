apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: spendpilot-prod
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/<your-org>/spendpilot-helm.git
      targetRevision: main
      path: charts/spendpilot
      helm:
        valueFiles:
          - $values/environments/prod/values/spendpilot-values.yaml
    - repoURL: https://github.com/<your-org>/spendpilot-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: spend-control
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
